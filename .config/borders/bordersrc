#!/bin/bash
source "$HOME/.config/colors/colors.sh"

options=(
	style=round
	width=2.5
	#width=4.5
	hidpi=on
	inactive_color="${BACKGROUND}"
	active_color="${ACCENT_SECONDARY}"
	blacklist=""
	order=above
)

borders "${options[@]}"
